# All dependencies are now managed in pyproject.toml
# Install with: pip install -e .
# For kokoro TTS support: pip install -e ".[kokoro]"
# For development tools: pip install -e ".[dev]"

# This file is kept for backwards compatibility
# To install from pyproject.toml, run: pip install -e .
